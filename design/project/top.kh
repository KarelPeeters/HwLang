module top ports(
    clk: in clock,
    rst: in async bool,

    sync(clk, async rst) {
        x: in uint(8),
        y: out uint(8),
        z: out uint(8),
    }
) {
    comb {
        y = x;
    }

    reg out z = 0;
    clocked(clk, async rst) {
        z = x;
    }
}
