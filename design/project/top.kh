// Test: very large array (might hang when computing 2^N for range)
fn test() -> bool {
    val arr: [bool; 99999999999999] = undef;
    return arr[0];
}

module top ports() {
    const r = test();
}
