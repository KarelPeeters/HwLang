import std::types::bool;
import std::types::bits;
import std::types::uint;
import std::io::print;

pub module top ports(
    clk: input clock,
    rst: input async bool,
    x: input sync(clk, rst) bits(4),
    y: output sync(clk, rst) bits(4),
) body {
    val n: uint = test(8);

    combinatorial {
         y = x;
    }

    clocked(clk, rst) {

    }
}

function test(n: uint): uint {
    for (i in 0..n) {
        print(i);
    };
    return n;
}
