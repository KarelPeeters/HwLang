type T = uint(8);

module top ports(
    clk: in clock,
    rst: in async bool,

    sync(clk, async rst) {
        x: in T,
        y: out T,
    }
) {
    clocked(clk, async rst) {
        reg wire y = 255;
        y = x;
    }
}
