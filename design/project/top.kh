import std.types.[bool, bits, uint, int_range];

pub module top ports(
    clk: in clock,
    rst: in async bool,
    x: in sync(clk, rst) bits(4),
    y: out sync(clk, rst) bits(4),
) body {
    reg r: sync(clk, rst) int_range(0..8) = 0;
    wire w: sync(clk, rst) int_range(0..8) = r;
}
